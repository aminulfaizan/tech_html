/* 
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/ClientSide/javascript.js to edit this template
 */


function demo(){
   var result = document.getElementById('result');
    
    var dateTime = new Date();
    var hours = dateTime.getHours();
     var minutes = dateTime.getMinutes();
      var sec = dateTime.getSeconds();
      
   var Watch =   hours+':' + minutes+':'+ sec;
      
      result.innerHTML = Watch;
}

setInterval(demo, 1000);

  
     
function validation(){
            var uname = document.login.uname.value;
            var pass = document.login.pass.value;
            if(uname==""|| pass==""){
                alert("both fields should not be blank");
                return;
            }
            if(pass.length<8 || pass.length>12){
                alert("password must be 8 to 12 char");
                return;
            }
            var str = uname.slice(0,1);//slice works like array. have question
             if(uname.slice(0,1)=="_" || uname.slice(0,1)=="@"||str.match(/[0-9]/g)!==null){
                alert("user name should not start with _,@ or number");
                return;
            }           
        }
        
 function  registration(){
            var rname = document.reg.rname.value;
            var remail = document.reg.remail.value;
             var rpassword = document.reg.rpassword.value;
             
            if(rname==""|| remail=="" || rpassword=="" ){
                alert(" fields should not be blank");
                return;
            }
            if( rpassword.length<8){
                alert("password must be more than 8 char ");
                return;
            }
            
            var str2 = rname.slice(0,1);//slice works like array. have question
             if(rname.slice(0,1)=="_" || rname.slice(0,1)=="@"||str2.match(/[0-9]/g)!==null){
                alert("user name should not start with _,@ or number");
                return;
            }
            
        }